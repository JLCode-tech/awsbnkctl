# ============================================================
# cne_instance — outer data sources (Sprint 3 AWS retarget)
#
# IBM resource-group / VPC lookups dropped — EKS surfaces the
# cluster shape via providers.tf data sources. Empty placeholder
# kept so consumers tracking the v0.x wrapper layout see the same
# data.tf seam.
# ============================================================
