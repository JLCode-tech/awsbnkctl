name: bnk-finance-agent
description: "A financial analysis agent that uses internal enterprise tools securely via F5 BNK."
runtime: python

tools:
  - name: internal-finance-mcp
    type: mcp
    config:
      endpoint: "http://bnk-ingress.aws.corp/v1/mcp/forecast"
      transport: sse

memory:
  enabled: true

identity:
  enabled: true
