# Terraform variable reference

Auto-generated by `go run ./tools/refgen/tfvars-md > book/src/29-terraform-variable-reference.md`. Re-run on every `terraform/variables.tf` change.

Every variable below is settable via `terraform.tfvars`, `-var`, `-var-file`, or (for sensitive values) the corresponding `TF_VAR_<name>` environment variable. Variables with `_required_` defaults must be set explicitly. See [Chapter 13](./13-terraform-variables.md) for how awsbnkctl threads these through the workspace config.

## Root module variables

Source: `terraform/variables.tf`

| Variable | Type | Default | Description | Sensitive |
|---|---|---|---|---|
| `region` | `string` | _required_ | AWS region for the EKS cluster (e.g. us-east-1) | no |
| `cluster_name` | `string` | _required_ | EKS cluster name | no |
| `cluster_version` | `string` | `"1.30"` | Kubernetes version (default 1.30 per PRD 07) | no |
| `vpc_id` | `string` | _required_ | VPC ID hosting the cluster (Sprint 1 v1.x adds create_vpc = true to provision a fresh one) | no |
| `subnet_ids` | `list(string)` | _required_ | Private subnet IDs (>=3 AZs per PRD 07 HA target) | no |
| `node_instance_types` | `list(string)` | `["c5n.4xlarge"]` | Self-managed node group instance types (default c5n.4xlarge per PRD 07 SR-IOV target) | no |
| `node_min_size` | `number` | `2` | Node group minimum size | no |
| `node_max_size` | `number` | `10` | Node group maximum size | no |
| `node_desired_size` | `number` | `3` | Node group initial size | no |
| `enable_multus` | `bool` | `true` | Install upstream Multus CNI DaemonSet (default true). See PRD 07 §\"Multus + SR-IOV stack\". | no |
| `enable_sriov` | `bool` | `true` | Install upstream SR-IOV CNI + device plugin DaemonSets (default true). See PRD 07 §\"Multus + SR-IOV stack\". | no |
| `sriov_resource_name` | `string` | `"intel.com/sriov"` | Resource key the SR-IOV device plugin advertises VFs under (default intel.com/sriov per PRD 07). | no |
| `workspace_name` | `string` | `"default"` | awsbnkctl workspace name; threaded into the s3_supply_chain bucket name + iam_irsa role name for inspectability. PRD 08 § \"Decision\". | no |
| `kms_key_arn` | `string` | `""` | Existing customer-managed KMS CMK for the supply-chain bucket. Empty creates one in-module (PRD 08 § \"Trade-offs accepted\"). | no |
| `far_auth_file_local_path` | `string` | _required_ | Local path to the FAR pull-key archive (f5cne-far-auth-*.tar.gz). Supplied by `awsbnkctl init`. | no |
| `jwt_file_local_path` | `string` | _required_ | Local path to the subscription JWT (f5cne-subscription-*.jwt). Supplied by `awsbnkctl init`. | no |
| `flo_namespace` | `string` | `"flo-system"` | Kubernetes namespace where the FLO service account lives. PRD 08 default flo-system. | no |
| `enable_ecr_mirror` | `bool` | `false` | Enable the optional ECR mirror module (PRD 08 v1.0 stretch). Default false; the module is a no-op when disabled. Set true to mirror FAR images into ECR for air-gapped deployments. | no |
| `cert_manager_namespace` | `string` | `"cert-manager"` | Kubernetes namespace where cert-manager is installed. Default 'cert-manager'. | no |
| `cert_manager_version` | `string` | `"v1.17.3"` | Helm chart version for cert-manager. Default per Sprint 3 module. | no |
| `far_repo_url` | `string` | `"repo.f5.com"` | FAR Repository URL for Docker and Helm registry. Default repo.f5.com. | no |
| `f5_bigip_k8s_manifest_version` | `string` | `"2.3.0-3.2598.3-0.0.170"` | Version of the f5-bigip-k8s-manifest chart (FLO + CIS versions are extracted from this). | no |
| `flo_utils_namespace` | `string` | `"f5-utils"` | Kubernetes namespace where the F5 utilities (License CR, etc.) live. Default 'f5-utils'. | no |
| `cneinstance_deployment_size` | `string` | `"Small"` | CNEInstance deployment size (Small \| Medium \| Large). | no |
| `cneinstance_gslb_datacenter_name` | `string` | `""` | GSLB datacenter name for CNEInstance (empty = no GSLB). | no |
| `license_mode` | `string` | `"connected"` | License operation mode (connected \| disconnected). | no |
| `deploy_bnk` | `bool` | `true` | Deploy BIG-IP Next for Kubernetes (FLO + CNEInstance + License). Default true. | no |
| `testing_create_cluster_jumphosts` | `bool` | `false` | Provision the testing-fixture jumphosts (one per supplied subnet). Default false. | no |
| `testing_ssh_key_name` | `string` | `""` | AWS-side EC2 key pair name to inject into the testing jumphosts. | no |
| `testing_jumphost_instance_type` | `string` | `"t3.medium"` | EC2 instance type for the testing jumphosts. | no |

## Module: `cert_manager`

Source: `terraform/modules/cert_manager/variables.tf`

| Variable | Type | Default | Description | Sensitive |
|---|---|---|---|---|
| `aws_region` | `string` | _required_ | AWS region hosting the EKS cluster | no |
| `eks_cluster_name` | `string` | _required_ | EKS cluster name (replaces roks_cluster_name_or_id from the inherited module) | no |
| `cert_manager_namespace` | `string` | `"cert-manager"` | Kubernetes namespace for cert-manager | no |
| `cert_manager_version` | `string` | `"v1.17.3"` | cert-manager Helm chart version | no |
| `create_eks_cluster` | `bool` | `false` | When true, cluster is being created by eks_cluster — skip plan-time cluster credential fetch | no |
| `eks_cluster_dependency_id` | `string` | `null` | eks_cluster sentinel ID — when set, defers runtime_config fetch to apply time after eks_cluster completes | no |
| `kubeconfig_dir` | `string` | `"/work/.bnk/scratch/kubeconfig/cert_manager"` | Persistent, writable dir reserved for local-exec scratch space. Inherited from the v0.x wrapper for source compatibility. | no |

## Module: `cne_instance`

Source: `terraform/modules/cne_instance/variables.tf`

| Variable | Type | Default | Description | Sensitive |
|---|---|---|---|---|
| `aws_region` | `string` | _required_ | AWS region hosting the EKS cluster | no |
| `eks_cluster_name` | `string` | _required_ | EKS cluster name (replaces roks_cluster_name_or_id) | no |
| `far_repo_url` | `string` | `"repo.f5.com"` | FAR Repository URL for Docker and Helm registry | no |
| `flo_namespace` | `string` | `"flo-system"` | Namespace for F5 Lifecycle Operator | no |
| `flo_utils_namespace` | `string` | `"f5-utils"` | Namespace for F5 utility components | no |
| `f5_bigip_k8s_manifest_version` | `string` | `"2.3.0-3.2598.3-0.0.170"` | Version of f5-bigip-k8s-manifest chart | no |
| `flo_irsa_role_arn` | `string` | `""` | IAM role ARN for the FLO IRSA service account (replaces flo_trusted_profile_id) | no |
| `flo_cluster_issuer_name` | `string` | `""` | mTLS certificate issuer name | no |
| `cneinstance_deployment_size` | `string` | `"Small"` | Deployment size for CNEInstance (Small, Medium, Large) | no |
| `cneinstance_gslb_datacenter_name` | `string` | `""` | GSLB datacenter name for CNEInstance (optional) | no |
| `cneinstance_network_attachments` | `list(string)` | `["ens5-ipvlan-l2", "macvlan-conf"]` | Multus Network Attachment Definitions for CNEInstance TMM deployments | no |
| `create_eks_cluster` | `bool` | `false` | When true, the EKS cluster is being created in the same apply — defers credential fetch to apply time | no |
| `eks_cluster_dependency_id` | `string` | `null` | eks_cluster sentinel ID — when set, defers runtime_config fetch to apply time after eks_cluster completes | no |
| `flo_dependency_id` | `string` | `null` | flo_ready sentinel ID — defer cne_instance until flo completes and CRDs are registered | no |
| `deploy_bnk` | `bool` | `true` | Deploy BIG-IP Next for Kubernetes; when false no CNEInstance resources are created | no |
| `kubeconfig_dir` | `string` | `"/work/.bnk/scratch/kubeconfig/cne_instance"` | Reserved scratch dir for any future local-exec staging. | no |

## Module: `ecr_mirror`

Source: `terraform/modules/ecr_mirror/variables.tf`

| Variable | Type | Default | Description | Sensitive |
|---|---|---|---|---|
| `region` | `string` | _required_ | AWS region; ECR is region-scoped. | no |
| `workspace_name` | `string` | _required_ | Workspace name; threaded into repository naming + tagging. | no |
| `enable_ecr_mirror` | `bool` | `false` | Master gate. Module emits nothing when false (default). When true, creates one ECR repository per image in var.images and runs skopeo via null_resource. v1.0 stretch (PRD 08). | no |
| `images` | `list(string)` | `[]` | Source images to mirror, as `<repo>`:`<tag>` pairs. Empty = the module emits the repositories but skips the skopeo step (operator runs it out-of-band). | no |
| `source_authfile_path` | `string` | `""` | Local path to a docker authfile (`skopeo login --authfile`) for the source registry. Empty = skopeo uses the ambient docker creds chain. | no |
| `tags` | `map(string)` | `{}` | Additional tags applied to every ECR repository. | no |

## Module: `eks_cluster`

Source: `terraform/modules/eks_cluster/variables.tf`

| Variable | Type | Default | Description | Sensitive |
|---|---|---|---|---|
| `region` | `string` | _required_ | AWS region for the EKS cluster (e.g. us-east-1) | no |
| `cluster_name` | `string` | _required_ | EKS cluster name | no |
| `cluster_version` | `string` | `"1.30"` | Kubernetes version (default 1.30 per PRD 07) | no |
| `vpc_id` | `string` | _required_ | VPC ID hosting the cluster (Sprint 1 v1.x adds create_vpc = true to provision a fresh one) | no |
| `subnet_ids` | `list(string)` | _required_ | Private subnet IDs (>=3 AZs per PRD 07 HA target) | no |
| `node_instance_types` | `list(string)` | `["c5n.4xlarge"]` | Self-managed node group instance types (default c5n.4xlarge per PRD 07 SR-IOV target) | no |
| `node_min_size` | `number` | `2` | Node group minimum size | no |
| `node_max_size` | `number` | `10` | Node group maximum size | no |
| `node_desired_size` | `number` | `3` | Node group initial size | no |
| `enable_multus` | `bool` | `true` | Install upstream Multus CNI DaemonSet (k8snetworkplumbingwg/multus-cni v4.x thick plugin). Required for SR-IOV chaining; default true per PRD 07. | no |
| `enable_sriov` | `bool` | `true` | Install upstream SR-IOV CNI + SR-IOV device plugin DaemonSets. Default true per PRD 07; turn off only if the spike surfaces a hypothesis mismatch. | no |
| `sriov_resource_name` | `string` | `"intel.com/sriov"` | Resource key the SR-IOV device plugin advertises VFs under (default intel.com/sriov per PRD 07; BNK's CNEInstance reconciler expects this key). | no |

## Module: `flo`

Source: `terraform/modules/flo/variables.tf`

| Variable | Type | Default | Description | Sensitive |
|---|---|---|---|---|
| `aws_region` | `string` | _required_ | AWS region hosting the EKS cluster | no |
| `eks_cluster_name` | `string` | _required_ | EKS cluster name (replaces roks_cluster_name_or_id) | no |
| `far_repo_url` | `string` | `"repo.f5.com"` | FAR Repository URL for Docker and Helm registry | no |
| `f5_bigip_k8s_manifest_version` | `string` | `"2.3.0-3.2598.3-0.0.170"` | Version of the f5-bigip-k8s-manifest chart (FLO/CIS versions extracted from this) | no |
| `s3_bucket_name` | `string` | _required_ | Name of the supply-chain S3 bucket holding the FAR archive + JWT (output of the s3_supply_chain module) | no |
| `s3_far_auth_object_key` | `string` | _required_ | S3 key of the FAR pull-key archive (output of the s3_supply_chain module) | no |
| `s3_jwt_object_key` | `string` | _required_ | S3 key of the subscription JWT (output of the s3_supply_chain module) | no |
| `flo_irsa_role_arn` | `string` | _required_ | IAM role ARN the FLO ServiceAccount is annotated with (output of the iam_irsa module) | no |
| `flo_namespace` | `string` | `"flo-system"` | Namespace for the F5 Lifecycle Operator | no |
| `flo_utils_namespace` | `string` | `"f5-utils"` | Namespace for F5 utility components | no |
| `flo_service_account_name` | `string` | `"flo-controller"` | FLO ServiceAccount name (IRSA-annotated) | no |
| `cert_manager_namespace` | `string` | `"cert-manager"` | Kubernetes namespace where cert-manager is installed (PRD 07 cert_manager output) | no |
| `create_eks_cluster` | `bool` | `false` | When true, the EKS cluster is being created in the same apply — defers credential fetch to apply time | no |
| `eks_cluster_dependency_id` | `string` | `null` | eks_cluster sentinel ID — defer FLO until eks_cluster completes | no |
| `cert_manager_dependency_id` | `string` | `null` | cert_manager ready sentinel ID — blocks FLO until cert-manager CRDs are registered | no |
| `deploy_bnk` | `bool` | `true` | Deploy BIG-IP Next for Kubernetes; when false no FLO resources are created | no |
| `kubeconfig_dir` | `string` | `"/work/.bnk/scratch/kubeconfig/flo"` | Reserved scratch dir for any future local-exec staging. | no |
| `scratch_dir` | `string` | `"/work/.bnk/scratch"` | Reserved scratch dir; carried for v0.x source compatibility. | no |

## Module: `iam_irsa`

Source: `terraform/modules/iam_irsa/variables.tf`

| Variable | Type | Default | Description | Sensitive |
|---|---|---|---|---|
| `region` | `string` | _required_ | AWS region (for tagging + resource names). | no |
| `cluster_name` | `string` | _required_ | EKS cluster name; threaded into the IAM role name for inspectability. | no |
| `oidc_provider_arn` | `string` | _required_ | OIDC provider ARN from the eks_cluster module (PRD 07 outputs). | no |
| `cluster_oidc_issuer_url` | `string` | _required_ | OIDC issuer URL from the eks_cluster module (PRD 07 outputs). The trust policy's StringEquals condition is keyed on the issuer hostname (no scheme). | no |
| `flo_namespace` | `string` | `"flo-system"` | Kubernetes namespace where the FLO service account lives. | no |
| `flo_service_account_name` | `string` | `"flo-controller"` | FLO service account name. | no |
| `s3_bucket_arn` | `string` | _required_ | Supply-chain bucket ARN (from the s3_supply_chain module). The role's permission policy grants s3:GetObject on `<arn>`/*. | no |
| `kms_key_arn` | `string` | _required_ | Supply-chain CMK ARN (from the s3_supply_chain module). The role's permission policy grants kms:Decrypt on this key so encrypted-at-rest objects can be read. | no |
| `role_name_override` | `string` | `""` | Override the generated IAM role name. Empty = awsbnkctl-`<cluster>`-flo-supply-chain-reader. | no |
| `tags` | `map(string)` | `{}` | Additional tags applied to the IAM role + policy. | no |

## Module: `license`

Source: `terraform/modules/license/variables.tf`

| Variable | Type | Default | Description | Sensitive |
|---|---|---|---|---|
| `aws_region` | `string` | _required_ | AWS region hosting the EKS cluster | no |
| `eks_cluster_name` | `string` | _required_ | EKS cluster name (replaces roks_cluster_name_or_id) | no |
| `s3_bucket_name` | `string` | _required_ | Supply-chain S3 bucket holding the subscription JWT (output of s3_supply_chain) | no |
| `s3_jwt_object_key` | `string` | _required_ | S3 key of the subscription JWT (output of s3_supply_chain) | no |
| `flo_irsa_role_arn` | `string` | `""` | FLO IRSA role ARN — the FLO pod assumes this role to GetObject the JWT from S3 via a presigned URL | no |
| `flo_utils_namespace` | `string` | `"f5-utils"` | Namespace for F5 utility components (License CR lives here) | no |
| `license_mode` | `string` | `"connected"` | License operation mode (connected or disconnected) | no |
| `create_eks_cluster` | `bool` | `false` | When true, the EKS cluster is being created in the same apply — defers credential fetch to apply time | no |
| `eks_cluster_dependency_id` | `string` | `null` | eks_cluster sentinel ID — when set, defers credential fetch to apply time | no |
| `cneinstance_dependency_id` | `string` | `null` | cneinstance_ready_id — ensures License CRD is available before applying License CR | no |
| `deploy_bnk` | `bool` | `true` | Deploy BIG-IP Next for Kubernetes; when false no License resources are created | no |
| `kubeconfig_dir` | `string` | `"/work/.bnk/scratch/kubeconfig/license"` | Reserved scratch dir for any future local-exec staging. | no |

## Module: `s3_supply_chain`

Source: `terraform/modules/s3_supply_chain/variables.tf`

| Variable | Type | Default | Description | Sensitive |
|---|---|---|---|---|
| `region` | `string` | _required_ | AWS region for the supply-chain bucket + KMS CMK. | no |
| `workspace_name` | `string` | _required_ | Workspace name; threaded into bucket naming + tagging (PRD 08 § \"Decision\"). | no |
| `kms_key_arn` | `string` | `""` | Existing CMK ARN for SSE-KMS. Empty creates a fresh CMK in this module. | no |
| `far_auth_file_local_path` | `string` | _required_ | Local path to the FAR pull-key archive (f5cne-far-auth-*.tar.gz). | no |
| `jwt_file_local_path` | `string` | _required_ | Local path to the subscription JWT (f5cne-subscription-*.jwt). | no |
| `bucket_name_override` | `string` | `""` | Override the generated bucket name. Empty = awsbnkctl-`<workspace>`-`<random>`. | no |
| `far_auth_object_key` | `string` | `"far-auth.tar.gz"` | S3 key for the FAR archive. Defaults to far-auth.tar.gz. | no |
| `jwt_object_key` | `string` | `"subscription.jwt"` | S3 key for the subscription JWT. Defaults to subscription.jwt. | no |
| `tags` | `map(string)` | `{}` | Additional tags applied to every resource. Module always adds awsbnkctl.io/managed-by + prd. | no |

## Module: `testing`

Source: `terraform/modules/testing/variables.tf`

| Variable | Type | Default | Description | Sensitive |
|---|---|---|---|---|
| `aws_region` | `string` | _required_ | AWS region for the test jumphost(s) | no |
| `eks_cluster_name` | `string` | _required_ | EKS cluster name the jumphosts target | no |
| `aws_vpc_id` | `string` | _required_ | VPC ID hosting the EKS cluster — jumphosts attach to subnets in this VPC | no |
| `aws_subnet_ids` | `list(string)` | _required_ | Subnet IDs (one per AZ) the jumphosts get placed in. >=1 required; HA jumphost-per-AZ uses len(aws_subnet_ids) instances. | no |
| `testing_create_cluster_jumphosts` | `bool` | `false` | Create one jumphost per supplied subnet (one per AZ) | no |
| `testing_ssh_key_name` | `string` | `""` | Name of an AWS-side EC2 key pair to inject into all jumphosts (empty = no SSH key, falls back to the generated jumphost_shared_key only) | no |
| `testing_jumphost_instance_type` | `string` | `"t3.medium"` | EC2 instance type for jumphosts (e.g., t3.medium) | no |
| `testing_cluster_jumphost_name_prefix` | `string` | `"tf-testing-jumphost-cluster"` | Name prefix for cluster jumphosts — subnet ID is appended (`<prefix>`-`<subnet-id>`) | no |
| `eks_cluster_dependency_id` | `string` | `null` | eks_cluster sentinel ID — when set, defers VPC data-source reads to apply time after eks_cluster completes | no |
| `create_eks_cluster` | `bool` | `false` | Set true when the EKS cluster is being created in this run — defers cluster-VPC-derived data sources | no |

