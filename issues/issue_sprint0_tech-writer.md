# Sprint 0 — tech writer issues

Read-only review + dogfooding pass for Sprint 0 ("identity rewrite + IBM strip + AWS stub"). Sibling agent issue files (architect, staff, validator) read first; this file cross-references their open items rather than duplicating them.

Dogfood result: `make build` succeeds; `./bin/awsbnkctl --help` prints the expected awsbnkctl command tree (renamed surface in place); `./bin/awsbnkctl --version` reports `awsbnkctl dev (commit 9a59806, built 2026-05-14T05:59:31Z)` with the JLCode-tech.github.io book URL; `./bin/awsbnkctl doctor` exits 0 with the only failure being the expected `⚠ workspace "default" not initialised` row. Binary identity rewrite is functionally complete.

---

## Issue 1: CONTRIBUTING.md is the unmodified inherited roksbnkctl file, but README.md links to it as the awsbnkctl onboarding doc
**Severity**: high
**Status**: open
**Description**: `CONTRIBUTING.md` title (line 1) reads "Contributing to roksbnkctl"; every example invokes `roksbnkctl …`, `ROKSBNKCTL_E2E_*` env vars, `IBMCLOUD_API_KEY`, `ibmcloud-cli`, `ghcr.io/jgruberf5/roksbnkctl-tools-*` image tags, `kind create cluster --name roksbnkctl-test`, and refers to "ROKS cluster + BNK deployment on IBM Cloud" as the e2e target (line 401). `README.md` line 109 directs first-time contributors to "[`CONTRIBUTING.md`](CONTRIBUTING.md) — how to contribute, run tests, add a chapter, and cut a release" — so the dogfooded path is: clean clone → `README.md` → click `CONTRIBUTING.md` → land on a document that describes a different tool against a different cloud. This is the load-bearing first-time-reader failure of Sprint 0's identity rewrite. The brief's "code/binary identity" check did not extend to `CONTRIBUTING.md`, which is why it slipped past the architect / staff / validator sweeps. None of the three sibling-agent issue files flag this — the gap is fresh.
**Files affected**: `/Users/j.lucia/Code/github/awsbnkctl/CONTRIBUTING.md` (whole file), referenced from `/Users/j.lucia/Code/github/awsbnkctl/README.md:109`
**Proposed fix**: Either (a) Sprint 0 integrator-pass: replace the title with "Contributing to awsbnkctl", swap `roksbnkctl` → `awsbnkctl` and `IBMCLOUD_API_KEY` → `AWS_PROFILE` / `AWS_ACCESS_KEY_ID` examples throughout, swap `ghcr.io/jgruberf5/roksbnkctl-tools-*` → `ghcr.io/JLCode-tech/awsbnkctl-tools-*`, and add a "scaffolding banner" similar to the one in `book/src/preface.md` flagging that the AWS-specific e2e walkthrough rehydrates in Sprint 4. Or (b) defer the full rewrite to Sprint 5 (book retarget) but at minimum land a top-of-file banner now warning the reader that the document describes the pre-fork roksbnkctl workflow and to mentally swap IBM Cloud → AWS, ROKS → EKS, COS → S3. Option (a) is the right call for a doc that the README sends every new contributor to.

## Issue 2: Milestone-tag inconsistency — PLAN.md uses v0.2 for Sprint 1, CHANGELOG/MIGRATING/README use v0.1
**Severity**: high
**Status**: open
**Description**: `docs/PLAN.md:12` defines M1 as Sprint 1 → tag `v0.2`. `CHANGELOG.md:15` has the heading `### Planned for v0.1.0 (Sprint 0 → Sprint 1)`. `MIGRATING.md:46` says "Until v0.1.0 ships, there is nothing to migrate between." `README.md:5` says "pre-v0.1". The three Sprint-0-facing docs treat `v0.1.0` as the next-release tag; PLAN.md treats `v0.2` as the next-release tag. The remaining PLAN.md milestone tags (`v0.3`/`v0.5`/`v0.7`/`v0.9`/`v1.0`) imply there is no `v0.1.x` line — Sprint 1 is supposed to cut `v0.2` directly. A reader who reaches v0.1.0 in CHANGELOG and then opens PLAN.md will not find that tag. The contradiction also bites the integrator at tag-cut time: which tag is correct? The brief's "sprint-count consistency: README, CHANGELOG, MIGRATING agree on the milestone tags (v0.2, v0.3, v0.5, v0.7, v0.9, v1.0)" check is the canonical statement that PLAN.md is right and the other three drift.
**Files affected**: `/Users/j.lucia/Code/github/awsbnkctl/CHANGELOG.md:15`, `/Users/j.lucia/Code/github/awsbnkctl/MIGRATING.md:46`, `/Users/j.lucia/Code/github/awsbnkctl/README.md:5,17`
**Proposed fix**: Pick one. The brief implies v0.2 is canonical. Suggested edits: `CHANGELOG.md:15` → `### Planned for v0.2 (Sprint 1)`; `MIGRATING.md:46` → "Until v0.2 ships, there is nothing to migrate between."; `README.md:5` → "pre-v0.2" (or keep "pre-v0.1" as a one-line semver-precedence-honouring nuance and add a clarifier — but that bites the reader). Lowest-touch: take the v0.2 line from PLAN.md as the source of truth and edit the three docs in the same commit that lands Issue 1's CONTRIBUTING fix.

## Issue 3: prompts/README.md says "six PRDs (00 through 05)" but eight are scoped and seven exist on disk
**Severity**: medium
**Status**: open
**Description**: `prompts/README.md:7` reads: *"[`docs/prd/`](../docs/prd/) — six PRDs (`00-OVERVIEW.md` through `05-E2E-TEST-PLAN.md`) defining the *what* per phase"*. The actual count: `docs/prd/` lists 00, 01, 02, 03, 04, 05, 06, 07 today (seven PRDs on disk; PRD 08 forward-referenced and queued for Sprint 2 per architect Issue 1). PRD 06 (CLUSTER-TRIAL-PHASE-SPLIT) is inherited from roksbnkctl and explicitly referenced in `docs/PLAN.md:5` and `docs/prd/00-OVERVIEW.md:43`; PRD 07 (EKS-CLUSTER-SRIOV) is the load-bearing Sprint 0/1 PRD authored in this fork. `prompts/README.md:53` repeats the drift: *"typically one or two of `docs/prd/01-*` through `docs/prd/05-*`"*. A first-time contributor reading prompts/README.md to understand the four-role sprint pattern will undercount the design surface and miss the load-bearing PRD 07.
**Files affected**: `/Users/j.lucia/Code/github/awsbnkctl/prompts/README.md:7,53`
**Proposed fix**: Edit line 7 to "eight PRDs (`00-OVERVIEW.md` through `08-S3-SUPPLY-CHAIN-IRSA.md`; PRD 08 lands in Sprint 2)" and line 53 to "typically one or two of `docs/prd/01-*` through `docs/prd/08-*`". Sprint 0 integrator pass, or fold with Issue 4 below.

## Issue 4: prompts/README.md still describes the project as roksbnkctl at /mnt/d/project/roksbnkctl/
**Severity**: medium
**Status**: open
**Description**: `prompts/README.md:35` says: *'one-line opener: "You are the X agent for Sprint N of the roksbnkctl project"'* — recommending future prompt-authors retain the roksbnkctl identity. `prompts/README.md:79` says: *"State the project location (`/mnt/d/project/roksbnkctl/`), point at the PRD it should read…"* — naming the pre-fork WSL path of the original author's environment, not the awsbnkctl repo location. Both lines are template guidance for the integrator authoring future sprint prompts; both will misguide any future agent dispatching. The Sprint 0 prompts files themselves (`prompts/sprint0/{architect,staff,validator,tech-writer}.md`) have been updated to say "awsbnkctl"; only the meta-template prose drifts. Inherited prompt directories for `prompts/sprint{1..9}/` carry the roksbnkctl content verbatim — those are scoped to later-sprint retarget work, not Sprint 0's concern, so flagging here just for awareness.
**Files affected**: `/Users/j.lucia/Code/github/awsbnkctl/prompts/README.md:35,79`
**Proposed fix**: Replace `"You are the X agent for Sprint N of the roksbnkctl project"` with `"You are the X agent for Sprint N of the awsbnkctl project"`; replace `/mnt/d/project/roksbnkctl/` with `/Users/j.lucia/Code/github/awsbnkctl/` (or a placeholder like `<repo-root>/` since the path is integrator-machine-specific). Sprint 0 integrator pass.

## Issue 5: doctor output still surfaces an `oc` (OpenShift) row even on AWS retarget
**Severity**: medium
**Status**: open
**Description**: `./bin/awsbnkctl doctor` reports a row for `oc` ("Red Hat OpenShift CLI"): `✓ oc /opt/homebrew/bin/oc (Client Version: 4.21.3) (internalised in `awsbnkctl k *` via client-go; host install used only when passthrough is convenient)`. EKS is not OpenShift, and per `docs/prd/00-OVERVIEW.md:39` PRD 02 inheritance explicitly states *"`oc` subset dropped (EKS isn't OpenShift)"*. A first-time AWS user running `awsbnkctl doctor` will read this row as "this tool expects me to have OpenShift CLI installed" — the right tone for an awsbnkctl doctor is "STS caller-identity / EKS describe-cluster permission" not "Red Hat OpenShift CLI". The check is informational (passes whether or not `oc` is present), so it does not block; it is a presentation issue rather than a functional bug. The companion `ibmcloud` row was correctly dropped (`internal/doctor/doctor.go:112` comment); the `oc` row was not. This is adjacent to staff Issue 3 (residual IBM coupling in cred/exec/config packages) but specifically about the doctor's user-visible row list.
**Files affected**: `/Users/j.lucia/Code/github/awsbnkctl/internal/doctor/doctor.go:111` (the `oc` informational row), `/Users/j.lucia/Code/github/awsbnkctl/internal/doctor/doctor.go:66-71` (the leading comment block enumerating "kubectl, oc, ibmcloud, iperf3, dig" — `oc` and `ibmcloud` should drop)
**Proposed fix**: Sprint 1 (or Sprint 4 when AWS-shaped doctor checks land per PLAN.md): drop the `oc` informational row. The comment block at lines 66-71 in `doctor.go` already shows the right pattern (the `ibmcloud` reference is annotated as removed); mirror that for `oc`. Cross-reference staff Issue 3: the `internal/cli/doctor_backend.go` ops-pod doctor checks (still Kubernetes-specific) are the next layer to retarget once Sprint 1 lands `internal/aws/` STS / EKS describe-cluster surface.

## Issue 6: README "Why fork roksbnkctl instead of starting clean" says "Three of the five Terraform modules" but the prose then lists four module names
**Severity**: low
**Status**: open
**Description**: `README.md:9` reads: *"Three of the five Terraform modules in roksbnkctl — `cert_manager`, `flo`, `cne_instance`, `license` — are pure Kubernetes manifests parameterised by namespace, image, and cert chain."* The cardinal "three" doesn't match the four module names enumerated. PRD 00 (`docs/prd/00-OVERVIEW.md:11`) gets it right: *"Three of roksbnkctl's five Terraform modules — `cert_manager`, `flo`, `cne_instance`, `license` —"* — same drift, same place. PLAN.md `docs/PLAN.md:14` lists four module names in the M3 row without a cardinal qualifier, which is correct. The roksbnkctl tree has five modules total — `cert_manager`, `flo`, `cne_instance`, `license`, `testing` (per `docs/PLAN.md:237`) — of which the four enumerated port unchanged and `testing` needs an input swap, so the right framing is "four of the five". Minor but a first-time reader doing the arithmetic gets a paragraph that doesn't add up.
**Files affected**: `/Users/j.lucia/Code/github/awsbnkctl/README.md:9`, `/Users/j.lucia/Code/github/awsbnkctl/docs/prd/00-OVERVIEW.md:11`
**Proposed fix**: Change "Three of the five" → "Four of the five" in both files; or leave "Three" and enumerate three of the four modules. Trivial wording. Defer to the next architect prose pass.

## Issue 7: MIGRATING.md cross-link target chapter 14 is the inherited roksbnkctl prose
**Severity**: low
**Status**: open
**Description**: `MIGRATING.md:16` reads: *"AWS credentials resolved via the standard chain (env → profile → instance role → SSO); see [Chapter 14 — Credentials resolver](./book/src/14-credentials-resolver.md) once written"*. The link **does resolve** (architect Issue 4 already notes the H1 was updated to "# Credentials and the AWS resolver chain") but the body still opens with *"`roksbnkctl` handles four kinds of secrets: an IBM Cloud API key, a kubeconfig, an SSH private key, and the Terraform state file"* and links to *"PRD 04 — credential propagation"* at the jgruberf5/roksbnkctl GitHub URL. A reader following the MIGRATING cross-link gets IBM-flavoured content. The "once written" hedge at MIGRATING.md:16 is honest but the cross-link itself shouldn't fire until the chapter is rewritten. Architect Issue 4 (book chapter bodies still describe roksbnkctl behaviour) covers the root cause; this is the consumer-side manifestation in MIGRATING.md. Sprint 5 closes this when the book retarget lands.
**Files affected**: `/Users/j.lucia/Code/github/awsbnkctl/MIGRATING.md:16`, `/Users/j.lucia/Code/github/awsbnkctl/book/src/14-credentials-resolver.md` (cross-references architect Issue 4)
**Proposed fix**: Two acceptable resolutions: (a) drop the cross-link until Sprint 5 fills in the chapter — replace with the inline AWS credential chain description; or (b) leave as-is and let architect Issue 4's Sprint 5 rewrite close both. Option (b) is the lower-touch path and aligns with the "scaffolding" framing at MIGRATING.md:7.

## Issue 8: README "What's in this repo" tree omits `MIGRATING.md`, `CONTRIBUTING.md`, `LICENSE`, `CHANGELOG.md`
**Severity**: low
**Status**: open
**Description**: `README.md:69-80` shows a tree of "what's in this repo" — it lists `cmd/`, `internal/`, `terraform/`, `tools/`, `book/`, `docs/`, `agents/`, `prompts/`, `scripts/`. The four top-level Markdown files (`MIGRATING.md`, `CONTRIBUTING.md`, `LICENSE`, `CHANGELOG.md`) are not in the tree, yet the README's own "Pointers" section (line 103-110) sends the reader to three of them. A first-time reader scanning the tree might conclude there is no migration guide or no contributing doc until they reach the bottom of the README. Cosmetic but a small first-time-reader stumble.
**Files affected**: `/Users/j.lucia/Code/github/awsbnkctl/README.md:69-80`
**Proposed fix**: Either add four lines to the tree (e.g. `├── README.md / CHANGELOG.md / MIGRATING.md / CONTRIBUTING.md   # top-level docs`) or note "plus the top-level READMEs (CHANGELOG, MIGRATING, CONTRIBUTING, LICENSE)" beneath the tree. Trivial.

## Issue 9: tools-images.yml workflow strategy.matrix carries one entry; brittle for first-run if iperf3 Dockerfile path drifts
**Severity**: low
**Status**: open
**Description**: `.github/workflows/tools-images.yml:42` is `image: [iperf3]` — a single-entry matrix. Functionally correct (validator Issue 2 documents the Sprint 1 follow-up to add an AWS-flavoured tools image to the matrix). What's untested today: on push to `main` after the Sprint 0 commit lands, the `build-push-action` step references `./tools/docker/${{ matrix.image }}/Dockerfile` which resolves to `./tools/docker/iperf3/Dockerfile`. That file exists (the inherited iperf3 Dockerfile survived the IBM strip), so the workflow should pass — but neither the staff agent nor the validator agent ran `docker build -f tools/docker/iperf3/Dockerfile .` against the post-strip context to confirm the BUILD_ARGS rename from `ROKSBNKCTL_VERSION` to `AWSBNKCTL_VERSION` (workflow line 64, 83, 98) doesn't break a Dockerfile that still expects the old arg name. If the iperf3 Dockerfile reads `ARG ROKSBNKCTL_VERSION`, the workflow will pass-build but the image will tag with an empty version string. Low severity because iperf3 is a runtime image not a release artefact, and because validator's verification step confirmed all workflow YAML parses.
**Files affected**: `/Users/j.lucia/Code/github/awsbnkctl/.github/workflows/tools-images.yml:64,83,98`, `/Users/j.lucia/Code/github/awsbnkctl/tools/docker/iperf3/Dockerfile` (not directly reviewed; spot-check before first tag push)
**Proposed fix**: Spot-check the iperf3 Dockerfile for `ARG ROKSBNKCTL_VERSION` / `ARG ROKSBNKCTL_COMMIT` and rename to `ARG AWSBNKCTL_VERSION` / `ARG AWSBNKCTL_COMMIT` if present. Trivial pre-push verification.

---

## Cross-references to sibling-agent issues

- **architect Issue 1** (PRD 08 forward-reference 404): noted; the broken link is intentional per fork-inheritance map. No tech-writer issue filed.
- **architect Issue 3** (preface.md still describes IBM Cloud audience): noted; tech-writer Issue 1 (CONTRIBUTING.md) is the analogous gap in a different surface.
- **architect Issue 4** (book chapter bodies still describe roksbnkctl): noted; tech-writer Issue 7 is the MIGRATING.md cross-link consequence of this.
- **architect Issue 7** (PLAN.md three-PRD enumeration): noted; similar shape to tech-writer Issue 6 (README "three of the five" cardinal mismatch).
- **staff Issue 3** (residual IBM coupling in cred/exec/config packages): noted; tech-writer Issue 5 (doctor `oc` row) is the user-visible projection of the same residual coupling.
- **staff Issue 4** (CLI verbs deleted; init/lifecycle as Sprint-1-defer stubs): noted; the post-strip stub error messages were not exercised in this review pass — first-time reader who runs `awsbnkctl up` gets an error from a Sprint-1 stub, which is correct.
- **staff Issue 5** (doctor_backend.go ops-pod surface dead-reference): noted; aligns with tech-writer Issue 5 (doctor `oc` row).
- **validator Issue 1** (cspell IBM-specific entries retained): noted; consistent with architect Issue 4's "preface scaffolding banner sets expectations" framing. The author of next sprint's cspell sweep should cross-reference.
- **validator Issue 3** (`tools/refgen/` import-path rewrite incomplete): noted; flagged by validator as staff-domain follow-up. tech-writer pass did not re-verify since validator's audit is authoritative.

## Gate-criteria audit (PLAN.md § Sprint 0 "Gate to Sprint 1")

| Gate criterion | Verdict |
|---|---|
| `awsbnkctl --help` runs and prints the expected command list | **met** — verified locally; full cobra command tree (init, up, plan, apply, down, doctor, status, k, test, tfvars, workspaces, etc.) is present |
| `awsbnkctl doctor` runs (may report "not implemented for AWS yet" — but no panics) | **met** — exits 0; reports the inherited terraform / helm / kubectl / iperf3 / dig rows; the workspace-not-initialised warning is the expected first-run state. Caveat: still reports an `oc` row (tech-writer Issue 5) and the underlying cred surface still says `ibmcloud api key` (staff Issue 3) |
| No `ibmcloud` / `roks` / `cos` string references remain in the build path | **partially met** — the four explicitly-deleted directories (`internal/ibm/`, `internal/cos/`, `terraform/modules/roks_cluster/`, `tools/docker/ibmcloud/`) are gone, but residual functional names in `internal/cred`, `internal/exec`, `internal/config` survive per staff Issue 3 and an `IBMCLOUD_API_KEY` env-var probe lives in the cred resolver. Per PLAN.md `### Risks`: "Some inherited tests assume IBM environment vars or live ROKS connectivity. Skip with `t.Skip(...)` rather than deleting" — staff agent followed this contract. Sprint 2 closes the cred adapter. |
| A working tree on `main` with one or more commits attributable to "fork + retarget" | **TBD-by-integrator-at-tag-cut** — the working tree is staged but uncommitted at the time of this review; the integrator commits after folding tech-writer findings (or filing them as open issues) per the agent-dispatch contract |
| `make build && make test && go vet ./...` all pass | **met for `make build`** — verified locally; staff agent's report stands as authoritative for `make test` and `go vet` per validator Issue 4's note that validator did not have Go installed |
| `mdbook build book/` still passes | **TBD** — neither architect nor validator had mdbook locally; tech-writer also did not run mdbook. First push to a `book/**`-touching branch will exercise `.github/workflows/book.yml` and surface any rendering issues. Architect Issue 2 carries this forward. |
