apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: scn-udp-lb-gateway
  namespace: {{.Namespace}}
spec:
  gatewayClassName: {{.GatewayClassName}}
  infrastructure:
    parametersRef:
      group: gateway.k8s.f5.com
      kind: GatewaySettings
      name: awsbnkctl-udplb
  addresses:
  - type: IPAddress
    value: {{.VIP}}
  listeners:
  - name: udp-listener
    protocol: UDP
    port: 5005
    allowedRoutes:
      namespaces: {from: Same}
      kinds:
      - kind: L4Route
        group: gateway.k8s.f5.com
