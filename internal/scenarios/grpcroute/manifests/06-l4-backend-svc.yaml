apiVersion: v1
kind: Service
metadata:
  name: grpcbin-l4-svc
  namespace: {{.Namespace}}
spec:
  selector:
    app: grpcbin-backend
  ports:
  - name: tcp-grpc
    port: 9000
    targetPort: 9000
    protocol: TCP
